let users = ["Ali", "Amir", "Talha", 'Admin', "Nasir"];
if (users.length == 0) {
    console.log('We need to find some users!')
}
users = []
if (users.length == 0) {
    console.log('We need to find some users!')
}